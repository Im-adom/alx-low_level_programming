#include "main.h"

/**
 * _abs - To compute the absolute value of an integer.
 *
 * @i: inputs number as an integer.
 *
 * Return: absolute value
 */
int _abs(int);
{
	if (i >= 0)
	{
		return (i);
	}
	else 
	{
		return (i * -1);
	}
}
