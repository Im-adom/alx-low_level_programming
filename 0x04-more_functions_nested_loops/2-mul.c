#include"main.h"

/**
 * mul- Multiplies two digits.
 * @a: The first number to multiply.
 * @b: The second number to multiply with first number.
 * Return: The result of the multiplication between
 * parameter  a and parameter b.
 */
int mul(int a, int b)
{
	return (a * b);
}
