#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * malloc_checked- allocates memory using malloc.
 * @b: input
 * Return: pointer to the allocated memory.
 */
void *malloc_checked(unsigned int b)
{
	unsigned int b;

	malloc_checked = (unsigned int) malloc(size of(unsigned int));

	return (0);
}
